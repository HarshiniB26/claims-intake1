[project]
name = "status-tracker-service"
version = "2.7.1"
description = "Operational status and work item tracking API"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
